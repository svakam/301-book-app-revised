<section>
  <img src="<%= book.image_url%>" alt="<%= book.title %>">
  <h2><%= book.title %></h2>
  <h4><%= book.author %></h4>
  <p class="book-summary"><%= book.summary %></p>
  <p><%= book.isbn %></p>
  <p><%= book.bookshelf %></p>
</section>